[package]
name = "cd-bookmarks"
version = "0.1.0"
edition = "2024"

[dependencies]
directories = "6.0.0"
regex = "1.12.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
